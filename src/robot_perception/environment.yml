name: robot_perception
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pyyaml>=6.0
  # Krytyczne zależności runtime: piny dla deterministycznego środowiska.
  - numpy=1.26.4
  - opencv=4.9.0
  - pip:
      - pydantic>=2.7
      - open3d==0.19.0
      - ultralytics==8.3.0
      - pupil-apriltags>=1.0.4
      - transforms3d>=0.4.2
